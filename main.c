#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include "libs/arvore.h"

int main(int argc, char *argv[]){
    int n, i;
    char opcao[14];     // Opção
    ArvoreInt a;        // Arvore de Inteiros

    // Inicializa a arvore
    inicializa(&a);

    // Numero de entradas
    scanf("%d", &n);
    for(i = 0; i < n; i++){
        
    }

    return EXIT_SUCCESS;
}